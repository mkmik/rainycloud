Changes by version:
===================

0.1.1
=====

Using fixed random seed for unit testing
Avoid using babudb as it works only when the partitions are prefix coded. Overhead of reloading csv is low when the job takes 20 secs though.


0.1.0
=====

using git flow
implement object passing for COMPSs

